# load("~/Desktop/ResearchRepo/mapapp/R/sysdata.rda")

# devtools::load_all() # automatically source files in R/ folder

# devtools::use_package("ggfortify") # add dependency to DESCRIPTION file
# USE: "package::fun()"

# devtools::use_data(hitter, overwrite = TRUE) # add `hitter` to data/
# devtools::use_data(InitialList, FinalList, internal = FALSE, overwrite = TRUE)

# devtools::use_build_ignore("mapit.R") # Exclude file/directory from bundled version

# devtools::document()

# install.packages("devtools")
# devtools::install_github('cwcomiskey/mapapp')
# library(mapapp)

# devtools::use_vignette("mapapp")
